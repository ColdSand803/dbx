# 未完成事项 - 2026-07-07

## 待收尾

- 列详情的“表格视图 / 格式化 JSON”需要再确认是否要做成整列级切换：当前重点已按最新反馈修正行详情，列详情还保留在表格内展示 JSON 字段格式化的逻辑。
- 最新 `DataGrid.vue` 行详情调整后，还需要重新跑一次 `pnpm typecheck` 和相关 vitest。当前环境的 Windows 沙箱 ACL/审批层拦截了验证命令。
- 需要启动桌面端做一次手工验收：右键查看行详情时，顶部切换应在表格和整行 JSON 之间切换；表格模式下只有包含 JSON 的字段在复制按钮后显示“格式化 JSON / 原始值”切换按钮。
- MongoDB 嵌套对象/数组对象字段级联筛选已实现基础结构和测试，后续可补真实库样例回归，覆盖数组对象、多层对象、混合类型字段的筛选体验。

## 提交说明

- 本文件用于记录今天尚未完成或尚未验证的事项。
- 提交和推送仍待执行，原因是当前环境对 `git status`、`git commit`、`git push` 的提升执行审批被拦截。
