# 未完成事项 - 2026-07-07

## 本轮已收尾

- 已根据 2026-07-07 的提交记录定位本文件对应提交：`905fb2a7 Improve Mongo field browsing and detail JSON views`。
- 已安装并恢复前端/测试依赖：`pnpm.cmd install --frozen-lockfile --ignore-scripts --config.confirmModulesPurge=false --reporter=append-only`。
- 已补跑 `DataGrid.vue` 行详情调整后的类型检查：`pnpm.cmd run typecheck` 通过。
- 已补跑 Mongo 字段级联筛选相关单测：`pnpm.cmd exec vitest run packages/app-tests/documentStoreProvider.test.ts`，1 个测试文件 / 10 个测试通过。
- 已补跑全量 vitest：`pnpm.cmd run test`，269 个测试文件 / 2101 个测试通过。
- 全量测试第一次失败的原因是依赖安装时跳过了 native install scripts，导致 `better-sqlite3` 缺少 `better_sqlite3.node`；已通过在包目录运行 install 脚本生成 binding，并复跑失败用例通过。

## 仍待人工确认

- 列详情的“表格视图 / 格式化 JSON”是否需要进一步改成更明确的整列级 JSON 视图仍待产品确认。当前代码保留了列详情表格，并在列内存在可格式化 JSON 时提供“表格视图 / 格式化 JSON”切换。
- 仍建议启动桌面端或 Web 端做一次手工验收：右键查看行详情时，顶部切换应在表格和整行 JSON 之间切换；表格模式下只有包含 JSON 的字段在复制按钮后显示“格式化 JSON / 原始值”切换按钮。
- MongoDB 嵌套对象/数组对象字段级联筛选已实现基础结构和自动化测试，后续可补真实库样例回归，覆盖数组对象、多层对象、混合类型字段的筛选体验。

## 提交状态备注

- 本轮开始时 `main...origin/main` 已同步，说明 2026-07-07 的提交已在当前分支/远端基线中。
- 本文件在本轮被更新，用于记录已完成验证与剩余人工确认项。
